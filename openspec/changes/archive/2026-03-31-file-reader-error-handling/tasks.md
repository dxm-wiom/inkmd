## 1. Error Handler

- [x] 1.1 Add `reader.onerror` handler to `readFileWeb()` in `src/components/file-input.js` that emits an error event
- [x] 1.2 Listen for the error event in `src/main.js` and show a toast message

## 2. Verify

- [x] 2.1 Confirm the app still loads files normally (no regression)
- [x] 2.2 Confirm the error path works by inspecting the code flow
